# Database Configuration
DB_HOST=khatabook-adit-ef94.j.aivencloud.com
DB_PORT=10570
DB_USER=avnadmin
DB_PASSWORD=your_password_here
DB_NAME=defaultdb

# SSL Mode Options:
# REQUIRED - Requires CA certificate (download from Aiven console)
# DISABLED - No SSL (not recommended for production)
# VERIFY_IDENTITY - SSL without CA certificate verification
DB_SSL_MODE=DISABLED

# Note: If using SSL REQUIRED mode, download the CA certificate from Aiven
# and place it in the config folder, then update database.php path accordingly